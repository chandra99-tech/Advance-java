
Adv. Java_Feb 25 Batch
aayushjadhav32@gmail.com Switch account
 
Draft saved
* Indicates required question
Email
*
aayushjahav32@gmail.com
Which method is used to save or update the state of the given instance from the underlying database? 
*
1 point
1. session.update()
2. session.keep()
3. session.saveOrUpdate()
4. session.Load()
doGet and doPost methods return
*
1 point
a. int
b. void
c. String
d. none of the above
Which is not DI?
*
1 point
1. Setter Based DI
2. Auto-wiring DI
3. Both of the above
4. None of the above.
Which of the following is correct about messaging in RESTful web services
*
1 point
1. A client sends a message in form of a HTTP Request and server responds in form of a HTTP Response.
2. These messages contain message data and metadata i.e. information about message itself.
3. Both of the above
4. None of the above.
Which of the following are tags of hibernate.cfg.xml?
*
1 point
1. SQL variant to generate
2. DTD
3. JDBC connection
4. All of the options
*
1 point
Option 1
The method getWriter returns an object of type PrintWriter. This class has println methods to generate output. Which of these classes define the getWriter method? 

*
1 point
1. HttpServletRequest
2. HttpServletResponse
3. ServletConfig
4. ServletContext
What is Spring MVC framework?

*
1 point
1. Spring MVC framework is Model-Value-Class architecture and used to bind model data with values.
2. The Spring web MVC framework provides model-view-controller architecture and ready components that can be used to develop flexible and loosely coupled web applications.
3. Spring MVC framework is used for Transaction management for Web Applications.
4. Spring MVC framework is used for AOP for Web Applications.
What are the method of Methods of PreparedStatement interface?
*
1 point
1. public void setInt()
2. public void setDouble()
3. public ResultSet executeQuery()
4. public void setFloat()
What is bean in Spring? 
*
1 point
1. A component
2. An Object
3. A class
4. A container
Which interface provides methods to execute queries with the database?
*
1 point
1. Connection interface
2. Statement interface
3. ResultSet interface
4. None of the above
@GetMapping(“/startPage”)

public ModelAndView pocExample() {

ModelAndView modelAndView = new ModelAndView(“X”);

ModelAndView.addObject(“y”,”z”);

Return modelAndView;

}

Suppose x is jsp, the preferred way to read the y value in the jsp y using

*
1 point
a. custom tags
b. scriplets
c. directives
d. actions
PRN
*
1 point
250845920014
Name
*
1 point
Ayush Ankush Jadhav
Which is Stereo Type Annotations?
*
1 point
1.@Autowired
2. @Controller
3. @PreDestroy
4. @Qualifier
   Which method of the servlet is/are called several times in its life? 

*
1 point
1. init()
2. doPost()
3. destroy()
4. None of above
Suppose you want to know each time request comes in, so that you can log it. Which listener is appropriate for that?
*
1 point
1. ServletRequestListener
2. ServletRequestAttributeListener
3. ServletContextListener
4. HttpRequestListener
Which of the following is not a scope in jsp

*
1 point
a. session
b. request
c. response
d. page
which of the following is true for postman
*
1 point
a. postman is used to test web services
b. postman is tool used to test our database queries
c. postman is tool used to test css and html
d. none of the above
Spring’s core transaction management abstraction is based on the interface _________ 
*
1 point
1. TransactionManager
2. PlatformTransactionManager
3. PlatformManager
4. PlatformTransaction
Which is not application context?

*
1 point
1. ClassPathXmlApplicationContext
2. AnnotationApplicationContext
3. WebApplicationContext
4. WebPathXmlApplicationContext
Suppose you want to know each time request comes in, so that you can log it. Which listener is appropriate for that? 
*
1 point
1. ServletContextAttributeListener
2. ServletContextListener
3. HttpSessionListener
4. All of these
Which of the following are the valid scopes in JSP?

*
1 point
1. request, page, context, application
2. response, page, session, application
3. request, page, session, global
4. request, page, session, application
What is the default scope of the beans?
*
1 point
1. Prototype
2. session
3. Request
4. Singleton
In Hibernate when we give

Xx = session.get(X.class, ?) here ? is

*
1 point
a. tablename
b. primary key value which is serial
c. databasename
d. none of the above
the configuration file used to set proxy information, in maven is called as 
*
1 point
a. pom.xml
b. settings.xml
c. proxy.xml
d. none of the above
What is dirty checking in Hibernate?

*
1 point
1. object state changes in order to synchronize the updated state with the database
2. remove the dirty data from database.
3. check the data when insert into database.
4. None
Which of the following is not a state of object in Hibernate?

*
1 point
1. Attached
2. Detached
3. Persistent
4. Transient
Which ways are used by the Log4j and Logback frameworks in hibernate framework to support logging?
*
1 point
1. By log4j.xml file
2. By log4j.properties
3. Both of above
4. None of the above
     Which of the following is not true for servlet? 

*
1 point
1. It is persistent
2. Platform independent
3. It is multi threaded
4. It is single threaded
<mapping = “class “?”/>   ? here contains
*
1 point
a. name of the dao class
b. name of the Entity class
c. name of the configuration file
d. none of the above
Which Standard tag is used store data in the JavaBeans instance?
*
1 point
1. <jsp:useBean>
2.  <jsp:setBean>
3.   <jsp:set>
4.  <jsp:setProperty>
How can we get context init parameter and run some code before rest of the application can service a client?
*
1 point
1. ServletContextAttributeListener
2. ServletContextListener
3. HttpSessionListener
4. All of these
Which object is used to forward the request processing from one servlet to another? 

*
1 point
1. ServeltContext
2. ServletConfig
3. RequestDispatcher
4. ResponseDispatcher
Element used to specify access attributes for bean’s methods.

*
1 point
1. security:spring
2. security:intercept-security
3. security:intercept
4. security:protect
The command to invoke some specific maven functionality in command line
*
1 point
a. maven
b. build
c. mvn
d. none of the above
In which file database table configuration is stored? 

*
1 point
1. .dbm
2. .hbm
3. .ora
4. .sql
Which of the following method can be used to used to instantiate a method?
*
1 point
1. static factory method
2. default-init method
3. destroy method
4. lazy-init method
Isolation Levels Supported by Spring:- 

*
1 point
1. DEFAULT
2. READ COMMITTED
3. READ UNCOMMITTED
4. All of the mentioned
What is @Controller annotation?
*
1 point
1. The @Controller annotation indicates that a particular class serves the role of a controller. 2. The @Controller annotation indicates how to control the transaction management.
3. The @Controller annotation indicates how to control the dependency injection.
4. The @Controller annotation indicates how to control the aspect programming.
The function present in CommandLineRunner interface in spring is
*
1 point
a. run
b. runner
c. runTask
d. none of the above
Which of the following element is used to represent one-to-one relationship in hibernate?
*
1 point
1. many-to-one
2. many-one
3. ManyToOne
4. None of the above
to add a cookie, in the web application we need to use object of
*
1 point
a. HttpServletRequest
b. HttpSession
c. HttpServletResponse
d. HttpCookie
Which of the following method can be used to used to instantiate a method?
*
1 point
1. static factory method
2. default-init method
3. destroy method
4. lazy-init method
Test, compile, verify in maven are called as 
*
1 point
a. phases
b. goals
c. lifecycle
d. none of the above
How to handle shut down of IoC container?
*
1 point
1. Using shutdownHook()
2. Using shutdownHandler()
3. Using registerHook()
4. Using registerShutdownHook()
Spring provides a factory that can export beans annotated with:- 

*
1 point
1. javax.jws.WebService
2. javax.jws.WebServiceProvider
3. all of the mentioned
4. none of the mentioned
org.hibernate.SessionFactory can manage contextual sessions and allows to retrieve them by:- 
*
1 point
1. getSession() method
2. getCurrent() method
3. getCurrentSession() method
4. None of the above
In which of these getAttibute() and setAttribute() method has defined? 

*
1 point
1. HttpSession
2. ServletRequest
3. ServletContext
4. All of these
@GetMapping and @PostMapping annotations used on 

*
1 point
a. functions
b. class
c. variables
d. none of the above
In the elements of Hibernate architecture is a factory of session and client of ConnectionProvider, It holds the second level cache (optional) of data is _____.
*
1 point
1. Session
2. ConnectionProvider
3. Transaction
4. SessionFactory
Which of these simplifies an Object Relational Mapping tool?

*
1 point
1. Data creation
2. Data manipulation
3. Data access
4. All mentioned above
 Factory to use our Spring bean as the implementation:-
*
1 point
1. jaxws:end
2. jaxws:endpoint
3. all of the mentioned
4. none of the mentioned
to handle 404 in jee web application of jee, we configure the file
*
1 point
a. http.xml
b. 404.xml
c. web.xml
d. none of the above
What is no mode of autowiring?
*
1 point
1. Autowiring by property name.
2. Similar to byType, but type applies to constructor arguments.
3. Default setting which means no autowiring and you should use explicit bean reference for wiring.
4. Spring first tries to wire using autowire by constructor, if it does not work,Spring tries to autowire by byType.
Which driver converts JDBC calls directly into the vendor-specific database protocol?
*
1 point
1. Native-API driver
2. Network Protocol driver
3. Thin driver
4. None of the above
=====================================================================
In the following code spring will create which objects 
@RestController

public class UserController{

@Autowired

private UserService userService;

}

*
1 point
a. UserService object
b. UserController object
c. UserController object and UserService object
d. none of the above
Page 1 of 1
Never submit passwords through Google Forms.
This content is neither created nor endorsed by Google. - Contact form owner - Terms of Service - Privacy Policy
Does this form look suspicious? Report

Google Forms